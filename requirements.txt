# Dipendenze di TypeTrace.
# Il file precedente era un "pip freeze" dell'intera macchina di sviluppo:
# 218 righe, con aider-chat, buildozer, Cython e una wheel di PyAudio ancorata
# a un percorso assoluto (E:/jarvis/...) che impediva alla build della CI di
# installare qualunque cosa.

PyQt6>=6.6
pynput>=1.7.6
pystray>=0.19.5
Pillow>=10.0
psutil>=5.9
